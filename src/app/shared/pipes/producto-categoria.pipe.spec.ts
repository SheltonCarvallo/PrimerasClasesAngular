import { ProductoCategoriaPipe } from './producto-categoria.pipe';

describe('ProductoCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductoCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
